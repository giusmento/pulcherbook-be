export enum PartnerStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
  PENDING = "pending",
}

export enum ShopStatus {
  ONLINE = "online",
  OFFLINE = "offline",
  DELETED = "deleted",
}

export enum ServiceStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}
